[project]
name = "roblox-vibe-code-speech"
version = "0.1.0"
description = "Roblox Studio MCP integration with Gemini"
dependencies = [
    "google-genai",
    "mcp",
    "python-dotenv",
]

[tool.uv]
managed = true

